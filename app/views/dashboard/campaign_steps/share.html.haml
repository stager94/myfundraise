.new-campaign-block
	= form_for @campaign, url: dashboard_campaign_step_path(campaign_id: @campaign.id) do |f|
		.campaign-container
			%h3 Share on Facebook
			
			.grayed.text-center
				= image_tag @campaign.photo.url(:cover), size: "300x200"
				.sample-campaign
					%h2= @campaign.title
					%p= campaign_url(@campaign)

			.actions.text-center
				= link_to 'Готово', @campaign, class: "btn btn-lg btn-default"
				/ = f.submit "Post to Facebook".html_safe, class: "btn btn-lg btn-fb"
